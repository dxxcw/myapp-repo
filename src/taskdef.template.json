{
  "family": "myapp-task",
  "networkMode": "awsvpc",
  "requiresCompatibilities": ["FARGATE"],
  "cpu": "256",
  "memory": "512",
  "executionRoleArn": "arn:aws:iam::369328295514:role/ecsTaskExecutionRole",
  "taskRoleArn": "arn:aws:iam::369328295514:role/ecsAppRole",
  "containerDefinitions": [
    {
      "name": "app",
      "image": "REPLACED_BY_BUILD",
      "portMappings": [{ "containerPort": 3000 }],
      "essential": true,
      "logConfiguration": {
        "logDriver": "awslogs",
        "options": {
          "awslogs-group": "/ecs/myapp",
          "awslogs-region": "ap-northeast-2",
          "awslogs-stream-prefix": "ecs"
        }
      }
    }
  ]
}

